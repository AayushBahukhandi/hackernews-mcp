[
  {
    "name": "get_stories",
    "description": "Get stories from Hacker News. The options are `top`, `new`, `ask_hn`, `show_hn` for types of stories. This doesn't include the comments. Use `get_story_info` to get the comments.",
    "arguments": [
      {
        "name": "story_type",
        "type": "string",
        "desc": "Type of stories to get, one of: `top`, `new`, `ask_hn`, `show_hn`",
        "default": "top"
      },
      {
        "name": "num_stories",
        "type": "integer",
        "desc": "Number of stories to get",
        "default": 10
      }
    ]
  },
  {
    "name": "get_story_info",
    "description": "Get detailed story info from Hacker News, including the comments",
    "arguments": [
      {
        "name": "story_id",
        "type": "integer",
        "desc": "Story ID",
        "required": true
      }
    ]
  },
  {
    "name": "search_stories",
    "description": "Search stories from Hacker News. It is generally recommended to use simpler queries to get a broader set of results (less than 5 words). Very targeted queries may not return any results.",
    "arguments": [
      {
        "name": "query",
        "type": "string",
        "desc": "Search query",
        "required": true
      },
      {
        "name": "search_by_date",
        "type": "boolean",
        "desc": "Search by date, defaults to false. If this is false, then we search by relevance, then points, then number of comments.",
        "default": false
      },
      {
        "name": "num_results",
        "type": "integer",
        "desc": "Number of results to get",
        "default": 10
      }
    ]
  },
  {
    "name": "get_user_info",
    "description": "Get user info from Hacker News, including the stories they've submitted",
    "arguments": [
      {
        "name": "user_name",
        "type": "string",
        "desc": "Username of the user",
        "required": true
      },
      {
        "name": "num_stories",
        "type": "integer",
        "desc": "Number of stories to get, defaults to 10",
        "default": 10
      }
    ]
  }
]
